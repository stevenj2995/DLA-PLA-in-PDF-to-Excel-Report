# Wajib
openpyxl>=3.1
PyMuPDF>=1.24
rapidfuzz>=3.0
scikit-learn>=1.3
streamlit>=1.30

# OCR untuk PDF hasil scan.
# Program Tesseract dipasang terpisah (sudah terpasang di laptop ini):
#   winget install UB-Mannheim.TesseractOCR
# Paket bahasa Indonesia diunduh ke C:\Program Files\Tesseract-OCR\tessdata\ind.traineddata
pytesseract>=0.3.10
Pillow>=10.0

# Pencocokan makna lintas bahasa. Model diunduh sekali saat pertama dipakai,
# setelah itu jalan offline. Tanpa ini program tetap jalan lewat jalur cadangan.
sentence-transformers>=3.0

# Backend untuk website publik (server.py + mulai.py). Tidak diperlukan
# kalau kamu hanya memakai app.py (Streamlit) atau run.py (CLI).
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9

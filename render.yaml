services:
  - type: web
    name: dla-to-excel
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/status
    envVars:
      # Backend ini terbuka di internet. Isi lewat dasbor Render, jangan di sini.
      - key: ACCESS_CODE
        sync: false
